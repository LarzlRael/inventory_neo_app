part of '../pages.dart';

class SellHistory extends StatelessWidget {
  const SellHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SellHistory'),
      ),
    );
  }
}
